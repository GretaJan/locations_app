import React from 'react';

const Worker = ({ name, surname }) => {

    return (
        <div>
            <h2>{ name }</h2>
            <h2>{ surname }</h2>
        </div>

    )
}

export default Worker